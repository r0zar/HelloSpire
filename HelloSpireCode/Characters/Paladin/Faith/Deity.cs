namespace HelloSpire.HelloSpireCode.Characters.PaladinContent.Faith;

/// <summary>The three deities Faith can be held in. Bane is deliberately absent from the
/// prototype; the fall is a later piece.</summary>
public enum Deity { Torm, Ilmater, Tyr }
